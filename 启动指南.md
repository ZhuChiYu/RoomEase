# 🚀 RoomEase 完整启动指南

## ❌ 当前问题

**Docker服务未运行** - 数据库无法连接

## ✅ 解决步骤

### 步骤 1: 启动 Docker Desktop ⚠️ **必须先做**

1. 打开 **Docker Desktop** 应用
   - 在 `应用程序` 文件夹中找到
   - 或使用 Spotlight 搜索 "Docker"

2. 等待 Docker 启动完成
   - 菜单栏的 Docker 图标变为**绿色**
   - 通常需要 10-30 秒

### 步骤 2: 运行一键启动脚本

打开终端，运行：

```bash
cd /Users/zhuchiyu/Project/RoomEasy/RoomEase
./check-and-start-all.sh
```

这个脚本会自动：
- ✅ 检查 Docker 是否运行
- ✅ 启动数据库容器
- ✅ 检查数据库连接
- ✅ 清理旧进程
- ✅ 启动后端服务

### 步骤 3: 重启移动App

在 Expo 终端按键：

```
r    # 重新加载
```

---

## 📊 预期输出

### ✅ 脚本成功运行
```
🔍 RoomEase 启动检查...

1️⃣ 检查 Docker...
   ✅ Docker 正在运行

2️⃣ 启动数据库容器...
   ✅ 数据库容器已启动

3️⃣ 检查数据库连接...
   ✅ 数据库连接正常

4️⃣ 清理旧进程...
   ✅ 无旧进程需要清理

5️⃣ 启动后端服务...
   📱 移动设备访问地址: http://192.168.31.221:4000
   💻 本地访问地址: http://localhost:4000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 API Gateway 启动成功！
📖 本地访问: http://localhost:4000/docs
📱 移动端访问: http://192.168.31.221:4000/docs
```

### ✅ App 自动登录成功
```
🔐 使用后端API，自动登录中...
📤 API请求: {
  baseURL: "http://192.168.31.221:4000",
  url: "/auth/login"
}
📥 API响应: { status: 200 }
✅ 自动登录成功
```

### ✅ 创建预订时的 API 调用
```
📝 [CreateOrder] 创建预订
🌐 [CreateOrder] 通过API创建预订...
📤 API请求: POST /reservations
📥 API响应: 201 Created
✅ [CreateOrder] API返回: {...}
```

---

## 🔧 手动启动方式（如果脚本失败）

### 1. 启动数据库
```bash
cd /Users/zhuchiyu/Project/RoomEasy/RoomEase
docker compose up -d
```

### 2. 启动后端
```bash
cd /Users/zhuchiyu/Project/RoomEasy/RoomEase
./start-backend-debug.sh
```

### 3. 启动移动App
```bash
cd /Users/zhuchiyu/Project/RoomEasy/RoomEase/apps/mobile
pnpm start
```

---

## 🐛 常见问题

### Q: Docker 启动失败
**A**: 确保 Docker Desktop 应用正在运行，菜单栏图标为绿色

### Q: 端口 4000 被占用
**A**: 运行清理命令
```bash
lsof -ti:4000 | xargs kill -9
```

### Q: 数据库连接失败
**A**: 检查 Docker 容器状态
```bash
docker compose ps
docker compose logs postgres
```

### Q: App 仍显示 "Network Error"
**A**: 完全重启 App
```bash
# 在 Expo 终端按 Ctrl+C 停止
cd apps/mobile
pnpm start --clear
```

---

## 📞 测试 API 连接

### 测试后端
```bash
# 测试健康检查（应该返回 404，说明服务运行正常）
curl http://192.168.31.221:4000

# 测试登录
curl -X POST http://192.168.31.221:4000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@demo.com","password":"123456"}'
```

### 测试账号
- **邮箱**: `admin@demo.com`
- **密码**: `123456`

---

## 🎯 完整工作流程

1. ✅ 启动 Docker Desktop
2. ✅ 运行 `./check-and-start-all.sh`
3. ✅ 在 Expo 终端按 `r` 重新加载
4. ✅ 观察日志，确认自动登录成功
5. ✅ 创建预订，测试 API 调用

---

## 💡 提示

- 确保手机/模拟器和开发机在**同一WiFi**
- 浏览器访问 http://192.168.31.221:4000/docs 查看 API 文档
- 如果 IP 地址变化，需要更新 `apps/mobile/app/config/environment.ts`


